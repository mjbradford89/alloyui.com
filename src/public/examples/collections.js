var modules = require('./modules');

module.exports = {
  all: [
    modules['ace-editor'],
    modules['carousel']
  ],

  recent: [
    modules['ace-editor'],
    modules['carousel']
  ],

  more: [
    modules['carousel']
  ]
};
